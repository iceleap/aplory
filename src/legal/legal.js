/* Legal pages are static text — this entry exists only to pull in the shared
   stylesheet so they inherit the site's theme, font and tokens. No framework
   ships to a page someone reads once. */
import "../base.css";
