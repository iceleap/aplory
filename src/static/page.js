/* Shared entry for the static pages (legal documents, 404). Its only job is to
   pull in the stylesheet so they inherit the site's theme, font and tokens —
   no framework ships to a page of plain text. */
import "../base.css";
